# 타입 안정성 강화 계획

> **작성일**: 2025-12-31  
> **목적**: any 타입 제거 및 타입 정의 일치

---

## 📋 현재 상태

- **any 타입 사용**: 121개 파일에서 발견
- **주요 문제 파일**:
  - `app/estimate/page.tsx` - `useState<any>`
  - `lib/decision/context-builder.ts` - 타입 정의 필요
  - `app/v5/analysis-phase2/page.tsx` - `any` 파라미터

---

## 🎯 목표

1. **any 타입 제거**
2. **타입 정의 일치 확인**
3. **옵셔널 필드 최소화**

---

## 📝 작업 전략

### Phase 1: 우선순위 파일 수정

**우선 수정 대상**:
1. `app/estimate/page.tsx` - `useState<any>` → 적절한 타입
2. `lib/decision/context-builder.ts` - 타입 정의
3. `app/v5/analysis-phase2/page.tsx` - 함수 파라미터 타입

### Phase 2: 점진적 개선

1. 파일별로 단계적 수정
2. 수정 후 테스트
3. 타입 에러 해결

---

## 🔧 수정 예시

### Before: any 사용

```typescript
const [estimateData, setEstimateData] = useState<any>(null)
```

### After: 타입 명시

```typescript
interface EstimateData {
  // 타입 정의
}

const [estimateData, setEstimateData] = useState<EstimateData | null>(null)
```

---

## ⚠️ 주의사항

1. **점진적 마이그레이션**
   - 한 번에 모든 파일 수정하지 않음
   - 파일별로 단계적 개선

2. **테스트**
   - 타입 수정 후 기능 테스트
   - 타입 에러 해결

---

## 📊 진행 상황

- [ ] Phase 1: 우선순위 파일 수정
- [ ] Phase 2: 점진적 개선

---

**작성 완료** ✅
