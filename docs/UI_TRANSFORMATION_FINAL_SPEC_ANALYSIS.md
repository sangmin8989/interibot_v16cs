# 성향 분석 결과 화면 전환 최종 명세서 분석 보고서

## 📋 명세서 개요

**작업 목표**: 성향 분석 결과 화면을 "추천형 설명 UI"에서 "결정 정리형 UI"로 전환

**핵심 제약사항**:
- ✅ UI 문구와 정보 구조만 변경
- ❌ AI 로직, 계산 로직, LOCK 로직 절대 수정 금지
- ❌ 디자인, 색상, 레이아웃 변경 금지

---

## 🎯 핵심 원칙 분석

### 원칙 1: 추천하지 않는다
- **현재 상태**: "추천 공정", "권장 공정" 표현 사용 중
- **변경 필요**: 모든 "추천", "권장", "제안" 표현 제거
- **대체 표현**: "남은 선택", "효과가 확실한 선택", "정리된 공정"

### 원칙 2: 선택지를 줄였다는 사실이 화면에서 보이게 한다
- **현재 상태**: 제외된 공정 정보가 화면에 표시되지 않음
- **변경 필요**: 
  - 제외된 공정 목록 명시적 표시
  - "검토하지 않아도 충분합니다" 메시지 추가
  - AI 판단 요약 박스로 제외/포함 공정 시각화

### 원칙 3: "이건 안 해도 된다"는 판단을 AI가 대신 정리해줬다는 인상
- **현재 상태**: 설명형 문구 중심
- **변경 필요**: 
  - "제외한 것"이 "고려한 것"보다 먼저 보이도록 구조 변경
  - "필요하지 않은 공사를 먼저 제외했습니다" 고정 문구 추가

---

## 📊 단계별 상세 분석 및 구현 계획

### [1단계] 상단 요약 문구 수정

**현재 코드 위치**: `app/onboarding/ai-recommendation/page.tsx:455-461`

**현재 문구**:
```tsx
<h1 className="text-2xl font-bold text-gray-900 mb-2">
  🎉 AI 분석 완료!
</h1>
<p className="text-gray-500">
  {spaceInfo?.pyeong}평 {spaceInfo?.housingType}의 {selectedSpaces.filter(s => s.isSelected).length}개 공간을 분석했습니다
</p>
```

**변경 후**:
```tsx
<h1 className="text-2xl font-bold text-gray-900 mb-2">
  이번 조건에서는<br />
  수납 외 공정은 검토하지 않도록 정리했습니다.
</h1>
<p className="text-sm text-gray-500">
  구조·설비 공사는 이번 분석에서 제외되었습니다.
</p>
```

**구현 난이도**: ⭐⭐ (동적 데이터 필요 - 제외된 공정 카테고리 추출)

**주의사항**:
- "수납 외 공정"은 동적으로 변경되어야 함 (실제 제외된 공정 카테고리 기반)
- API 응답에서 제외된 공정 정보를 추출해야 함

---

### [2단계] V3 분석 카드 문구 수정

**현재 코드 위치**: `app/onboarding/ai-recommendation/page.tsx:467-482`

**현재 문구**:
```tsx
<h2 className="text-2xl font-bold mb-3">{analysisResult.summary.title}</h2>
<p className="text-lg leading-relaxed text-white/95">
  {analysisResult.summary.description}
</p>
```

**변경 후**:
```tsx
<h2 className="text-2xl font-bold mb-3">
  {spaceInfo?.pyeong}평 · {spaceInfo?.totalPeople || spaceInfo?.familySizeRange}가구 기준<br />
  '{getFocusedCategory(analysisResult)}만 집중' 판단 결과입니다.
</h2>
<p className="text-lg leading-relaxed text-white/95">
  이번 분석은 생활 불편 요소 중<br />
  {getFocusedCategory(analysisResult)} 문제만 해결하도록 범위를 제한했습니다.
</p>
```

**구현 난이도**: ⭐⭐⭐ (동적 카테고리 추출 함수 필요)

**주의사항**:
- `getFocusedCategory()` 함수 구현 필요
- `analysisResult.needs` 또는 `analysisResult.processes`에서 집중 카테고리 추출
- API 응답 구조 확인 필요

---

### [3단계] '왜 이런 설계가 나왔나요?' 구조 전면 개편

**현재 코드 위치**: `app/onboarding/ai-recommendation/page.tsx:635-658`

**현재 구조**:
```tsx
<h2>왜 이런 설계가 나왔을까요?</h2>
{analysisResult.explanation.segments.map((segment, i) => (
  <div>
    <h3>{segment.order}. {segment.title}</h3>
    <div>{segment.content}</div>
  </div>
))}
```

**변경 후 구조**:
```tsx
<h2>왜 이런 설계가 나왔나요?</h2>

{/* [1] 이번 분석에서 고려한 것 */}
<div>
  <h3>이번 분석에서 고려한 것</h3>
  <ul>
    <li>가족 구성</li>
    <li>수납 부족</li>
    <li>사용 빈도 높은 공간</li>
  </ul>
</div>

{/* [2] 이번 분석에서 제외한 것 */}
<div>
  <h3>이번 분석에서 제외한 것</h3>
  <ul>
    <li>구조 변경 공사</li>
    <li>주방 리모델링</li>
    <li>욕실 전체 교체</li>
  </ul>
  <p className="font-semibold">
    이번 분석에서는<br />
    필요하지 않은 공사를 먼저 제외했습니다.
  </p>
</div>

{/* [3] 그래서 남은 선택 */}
<div>
  <h3>그래서 남은 선택</h3>
  <ul>
    {analysisResult.processes
      .filter(p => p.priority === 'must')
      .map(p => (
        <li>{p.name} (필수)</li>
      ))}
    {analysisResult.processes
      .filter(p => p.priority === 'recommended')
      .map(p => (
        <li>{p.name} (선택)</li>
      ))}
  </ul>
</div>
```

**구현 난이도**: ⭐⭐⭐⭐ (API 응답 구조 재구성 또는 클라이언트 측 변환 필요)

**주의사항**:
- `analysisResult.explanation.segments`를 3단 구조로 변환하는 함수 필요
- 제외된 공정 목록은 API에서 제공되지 않을 수 있음 → 클라이언트 측 계산 필요
- "고려한 것"은 `analysisResult.needs` 또는 `explanation.segments`에서 추출
- "제외한 것"은 전체 공정 목록에서 선택된 공정을 제외하여 계산

---

### [4단계] 'AI 판단 요약' 박스 신규 추가

**위치**: 추천 공정 영역 상단 (`app/onboarding/ai-recommendation/page.tsx:684` 이전)

**신규 컴포넌트**:
```tsx
<motion.div className="bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl p-6 border-2 border-gray-200 shadow-lg">
  <h2 className="text-lg font-bold text-gray-900 mb-4 flex items-center gap-2">
    🧠 인테리봇 판단 요약
  </h2>
  
  <div className="space-y-2">
    {getAllProcessCategories().map(category => {
      const isIncluded = isProcessIncluded(category, analysisResult.processes)
      return (
        <div className="flex items-center gap-2">
          <span>{isIncluded ? '⭕' : '❌'}</span>
          <span>{category}</span>
        </div>
      )
    })}
  </div>
  
  <p className="text-sm text-gray-600 mt-4 pt-4 border-t border-gray-200">
    이번 조건에서는<br />
    위 공정들을 검토하지 않아도 충분합니다.
  </p>
</motion.div>
```

**구현 난이도**: ⭐⭐⭐ (전체 공정 카테고리 목록 및 포함 여부 판단 로직 필요)

**주의사항**:
- 전체 공정 카테고리 목록 정의 필요 (구조 변경, 주방 리모델링, 욕실 전체 공사, 수납 강화, 마감 교체 등)
- `analysisResult.processes`와 비교하여 포함/제외 여부 판단
- API 응답에 없는 공정도 표시해야 함 (제외된 공정)

---

### [5단계] '제외된 공정' 접힘 영역 추가

**위치**: 추천 공정 영역 하단 또는 별도 섹션

**신규 컴포넌트**:
```tsx
<details className="bg-white rounded-2xl p-6 shadow-lg border border-gray-100">
  <summary className="text-lg font-bold text-gray-900 mb-4 cursor-pointer">
    이번 분석에서 제외한 공정
  </summary>
  
  <div className="mt-4 space-y-2">
    {getExcludedProcesses(analysisResult).map(process => (
      <div className="p-3 bg-gray-50 rounded-lg border border-gray-200">
        <span className="text-gray-700">{process}</span>
      </div>
    ))}
  </div>
  
  <p className="text-sm text-gray-500 mt-4">
    필요해지면 이후 단계에서 다시 선택할 수 있습니다.
  </p>
</details>
```

**구현 난이도**: ⭐⭐⭐⭐ (제외된 공정 계산 로직 필요)

**주의사항**:
- 전체 공정 목록에서 선택된 공정을 제외하여 계산
- 공정 카테고리 매핑 필요 (예: "주방 전체 리모델링", "방 마감 교체" 등)
- API 응답에 제외된 공정 정보가 없을 수 있음 → 클라이언트 측 계산

---

### [6단계] 추천 공정 문구 톤 수정

**현재 코드 위치**: `app/onboarding/ai-recommendation/page.tsx:696-739`

**현재 문구**:
```tsx
<h2>추천 공정</h2>
<h3>필수 공정</h3>
<h3>권장 공정</h3>
<p>{process.reason}</p>
```

**변경 후**:
```tsx
<h2>남은 선택</h2> {/* 또는 "정리된 공정" */}
<h3>필수 선택</h3>
<h3>선택 가능</h3>
<p>
  {process.name}은<br />
  이번 조건에서 가장 효과가 확실한 선택입니다.
</p>
<p className="text-xs text-gray-500">
  다른 공정 없이도 체감 변화가 큽니다.
</p>
```

**구현 난이도**: ⭐⭐ (문구만 변경)

**주의사항**:
- "추천 공정" → "남은 선택" 또는 "정리된 공정"
- "권장 공정" → "선택 가능"
- `process.reason` 대신 고정 문구 사용 또는 reason을 변환

---

### [7단계] 공통 금지 사항 확인

**금지 표현 검색 대상**:
- "추천합니다", "제안합니다", "권장합니다"
- "맞춤 설계", "추천되었습니다"
- 선택지를 늘리는 뉘앙스

**현재 코드에서 발견된 금지 표현**:
1. `app/onboarding/ai-recommendation/page.tsx:693`: "추천 공정"
2. `app/onboarding/ai-recommendation/page.tsx:724`: "권장 공정"
3. `app/onboarding/ai-recommendation/page.tsx:247`: "최종 추천 공정 정리 중..."
4. `app/onboarding/ai-recommendation/page.tsx:247`: "추천 공정 정리 중..."

**수정 필요 위치**: 모두 수정 필요

---

## 🔧 구현 전략

### Phase 1: 데이터 구조 분석 및 헬퍼 함수 구현

1. **제외된 공정 계산 로직**
   ```typescript
   // 전체 공정 카테고리 정의
   const ALL_PROCESS_CATEGORIES = [
     '구조 변경',
     '주방 리모델링',
     '욕실 전체 공사',
     '수납 강화',
     '마감 교체',
     '전기 증설 공사',
     // ...
   ]
   
   // 제외된 공정 계산
   function getExcludedProcesses(result: V31AnalysisResult): string[] {
     const includedCategories = result.processes.map(p => p.category)
     return ALL_PROCESS_CATEGORIES.filter(cat => !includedCategories.includes(cat))
   }
   ```

2. **집중 카테고리 추출**
   ```typescript
   function getFocusedCategory(result: V31AnalysisResult): string {
     // needs에서 high level인 것 중 가장 우선순위 높은 것
     const topNeed = result.needs
       .filter(n => n.level === 'high')
       .sort((a, b) => b.priority - a.priority)[0]
     return topNeed?.name || '수납'
   }
   ```

3. **공정 포함 여부 판단**
   ```typescript
   function isProcessIncluded(category: string, processes: Process[]): boolean {
     return processes.some(p => p.category === category || p.name.includes(category))
   }
   ```

### Phase 2: UI 컴포넌트 수정

1. **상단 요약 문구 수정** (1단계)
2. **V3 분석 카드 수정** (2단계)
3. **설계 설명 섹션 구조 변경** (3단계)
4. **AI 판단 요약 박스 추가** (4단계)
5. **제외된 공정 접힘 영역 추가** (5단계)
6. **추천 공정 문구 수정** (6단계)

### Phase 3: 금지 표현 제거

- 모든 "추천", "권장", "제안" 표현 검색 및 제거
- 선택지를 늘리는 뉘앙스 제거

---

## ⚠️ 주요 이슈 및 해결 방안

### 이슈 1: 제외된 공정 정보가 API 응답에 없음

**문제**: API 응답(`V31AnalysisResult`)에 제외된 공정 목록이 포함되지 않을 수 있음

**해결 방안**:
1. 전체 공정 카테고리 목록을 클라이언트에 정의
2. 선택된 공정(`analysisResult.processes`)과 비교하여 제외된 공정 계산
3. 공정 카테고리 매핑 테이블 생성

### 이슈 2: 집중 카테고리 동적 추출

**문제**: "수납만 집중" 같은 문구를 동적으로 생성해야 함

**해결 방안**:
1. `analysisResult.needs`에서 `level === 'high'`인 것 중 가장 우선순위 높은 것 선택
2. 또는 `analysisResult.processes`에서 가장 많이 언급된 카테고리 선택
3. Fallback: "수납" (기본값)

### 이슈 3: "고려한 것" / "제외한 것" 데이터 추출

**문제**: API 응답 구조가 3단 구조를 지원하지 않을 수 있음

**해결 방안**:
1. `analysisResult.explanation.segments`를 파싱하여 변환
2. 또는 클라이언트 측에서 `needs`와 `processes`를 분석하여 생성
3. API 응답 구조 확인 후 결정

---

## 📝 구현 체크리스트

### 필수 구현 항목
- [ ] 1단계: 상단 요약 문구 수정
- [ ] 2단계: V3 분석 카드 문구 수정
- [ ] 3단계: '왜 이런 설계가 나왔나요?' 구조 개편
- [ ] 4단계: AI 판단 요약 박스 추가
- [ ] 5단계: 제외된 공정 접힘 영역 추가
- [ ] 6단계: 추천 공정 문구 톤 수정
- [ ] 7단계: 금지 표현 제거

### 헬퍼 함수 구현
- [ ] `getExcludedProcesses()` - 제외된 공정 계산
- [ ] `getFocusedCategory()` - 집중 카테고리 추출
- [ ] `isProcessIncluded()` - 공정 포함 여부 판단
- [ ] `transformExplanationToThreeParts()` - 설명을 3단 구조로 변환

### 데이터 정의
- [ ] 전체 공정 카테고리 목록 정의
- [ ] 공정 카테고리 매핑 테이블
- [ ] 제외된 공정 표시명 매핑

---

## ✅ 완료 기준 검증

### 검증 항목
1. **"이건 안 해도 되는구나" 즉시 이해**
   - 제외된 공정이 화면에 명확히 표시되는가?
   - "검토하지 않아도 충분합니다" 메시지가 보이는가?

2. **'추천'보다 '제외'가 먼저 인식**
   - AI 판단 요약 박스가 추천 공정보다 위에 위치하는가?
   - 제외된 공정 목록이 접힘 영역으로 표시되는가?

3. **"판단을 정리해주는 AI"로 인식**
   - "필요하지 않은 공사를 먼저 제외했습니다" 문구가 있는가?
   - 설명 구조가 "제외한 것" → "고려한 것" → "남은 선택" 순서인가?

---

**작성일**: 2024년
**작성자**: AI Assistant
**버전**: 1.0












