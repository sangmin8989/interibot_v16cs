# 인테리봇 프로젝트 구조 정리

> 생성일: 2024-12-XX  
> 프로젝트 전체 구조 및 V5 관련 파일 목록 정리

---

## 1. 프로젝트 전체 폴더 구조

### 📁 루트 디렉토리
```
interibot/
├── app/                    # Next.js App Router 페이지 및 API
├── components/             # React 컴포넌트
├── lib/                    # 라이브러리 및 유틸리티
├── prisma/                 # Prisma 스키마 및 DB
├── types/                  # TypeScript 타입 정의
├── constants/              # 상수 정의
├── docs/                   # 문서
├── scripts/                # 스크립트 파일
├── tests/                  # 테스트 파일
└── assets/                 # 이미지 등 정적 자원
```

### 📁 app/ 폴더 구조
```
app/
├── api/                    # API 라우트
│   ├── v5/                 # V5 API 엔드포인트
│   ├── analysis/           # 분석 API
│   ├── analyze/            # 분석 처리 API
│   ├── estimate/           # 견적 API
│   ├── generate-questions/ # 질문 생성 API
│   └── ...
├── v5/                     # V5 페이지
│   ├── page.tsx            # V5 메인 페이지
│   ├── process-select/     # 공정 선택 페이지
│   ├── process-detail/     # 세부 공정 선택 페이지
│   └── options/            # 옵션 선택 페이지
├── onboarding/             # 온보딩 플로우
│   ├── process/            # 공정 선택 (구버전)
│   ├── process-new/        # 공정 선택 (신버전)
│   ├── personality/        # 성향 분석
│   ├── scope/              # 범위 선택
│   ├── detail-options/     # 세부 옵션
│   ├── estimate/          # 견적 결과
│   └── ...
├── result/                 # 결과 페이지
├── report/                 # 리포트 페이지
└── ...
```

### 📁 components/ 폴더 구조
```
components/
├── v5-ultimate/            # V5 Ultimate 컴포넌트
│   ├── ChatOnboarding.tsx
│   ├── DNAResultDisplay.tsx
│   ├── GradeSelector.tsx
│   ├── MoodSelector.tsx
│   ├── PhotoUploader.tsx
│   ├── SpaceInfoForm.tsx
│   ├── SixIndexDashboard.tsx
│   └── ShareButtons.tsx
├── v5/                     # V5 컴포넌트
│   ├── DNAResultCard.tsx
│   ├── ResultPage.tsx
│   ├── TagChipList.tsx
│   └── report/             # 리포트 컴포넌트
├── onboarding/             # 온보딩 컴포넌트
│   ├── process/            # 공정 관련
│   ├── options/            # 옵션 패널
│   └── ...
├── estimate/               # 견적 컴포넌트
├── analysis/               # 분석 컴포넌트
└── ...
```

### 📁 lib/ 폴더 구조
```
lib/
├── store/                  # 상태 관리 (Zustand)
│   ├── v5UltimateStore.ts
│   ├── personalityV5.store.ts
│   ├── processStore.ts
│   ├── scopeStore.ts
│   └── spaceInfoStore.ts
├── analysis/               # 분석 엔진
│   ├── v5/                 # V5 분석 엔진
│   ├── v5-ultimate/        # V5 Ultimate 분석
│   ├── engine-v3/          # V3 엔진
│   ├── engine-v3.1-core/    # V3.1 코어 엔진
│   └── ...
├── estimate/               # 견적 계산
│   ├── estimate-v4/        # V4 견적 시스템
│   └── ...
├── data/                   # 데이터 파일
├── db/                     # DB 어댑터
├── types/                  # 타입 정의
├── utils/                  # 유틸리티
└── ...
```

---

## 2. V5 관련 파일 목록

### 📁 app/v5/ 폴더 전체
```
app/v5/
├── page.tsx                # V5 메인 페이지 (무드/사진/채팅/결과)
├── process-select/
│   └── page.tsx            # 공정 선택 페이지
├── process-detail/
│   └── page.tsx            # 세부 공정 옵션 선택 페이지
└── options/
    └── page.tsx            # 등급 및 옵션 선택 페이지
```

**주요 기능:**
- `page.tsx`: 무드 선택 → 사진 업로드 → 채팅 → DNA 결과 표시
- `process-select/page.tsx`: 공정 선택 (철거, 주방, 욕실, 타일, 목공, 전기, 도배, 필름, 기타)
- `process-detail/page.tsx`: 주방/욕실/목공 세부 옵션 선택
- `options/page.tsx`: 등급 선택 (ESSENTIAL, STANDARD, OPUS)

### 📁 components/v5-ultimate/ 폴더 전체
```
components/v5-ultimate/
├── AnalyzingLoader.tsx     # 분석 중 로딩 컴포넌트
├── ChatOnboarding.tsx      # 채팅 온보딩 컴포넌트
├── DNAResultDisplay.tsx    # DNA 결과 표시
├── GradeSelector.tsx       # 등급 선택 컴포넌트
├── MoodSelector.tsx        # 무드 선택 컴포넌트
├── PhotoUploader.tsx       # 사진 업로드 컴포넌트
├── ShareButtons.tsx        # 공유 버튼
├── SixIndexDashboard.tsx   # 6가지 지수 대시보드
└── SpaceInfoForm.tsx       # 공간 정보 입력 폼
```

### 📁 lib/store/ 폴더 전체
```
lib/store/
├── personalityStore.ts     # 성향 분석 스토어 (구버전)
├── personalityV5.store.ts  # V5 성향 분석 스토어
├── processStore.ts         # 공정 선택 스토어
├── scopeStore.ts           # 범위 선택 스토어
├── spaceInfoStore.ts       # 공간 정보 스토어
└── v5UltimateStore.ts      # V5 Ultimate 통합 스토어
```

### 📁 lib/analysis/ 폴더 전체
```
lib/analysis/
├── v5/                     # V5 분석 엔진
│   ├── adapters/           # 어댑터
│   │   ├── v4-to-v5-input.ts
│   │   ├── v5-to-estimate-actions.ts
│   │   └── v5-to-v31-needs.ts
│   ├── audit/              # 감사 로깅
│   │   └── audit-logger.ts
│   ├── dna/                # DNA 결정 로직
│   │   ├── dna-determiner.ts
│   │   ├── dna-explain-bridge.ts
│   │   └── dna-types.ts
│   ├── estimate/           # 견적 정책
│   │   ├── index.ts
│   │   ├── policies.ts
│   │   ├── tag-to-contingency-policy.ts
│   │   ├── tag-to-grade-policy.ts
│   │   └── tag-to-material-policy.ts
│   ├── explain/            # 설명 생성
│   │   ├── index.ts
│   │   ├── process-explainer.ts
│   │   ├── summary-builder.ts
│   │   ├── tag-explainer.ts
│   │   └── templates.ts
│   ├── guards/             # 가드 (검증)
│   │   ├── input-guard.ts
│   │   ├── operational-guard.ts
│   │   └── reproducibility-guard.ts
│   ├── argen-material-connector.ts
│   ├── argen-recommender.ts
│   ├── choice-paralysis.ts
│   ├── hypothesis-generator.ts
│   ├── index.ts
│   ├── input-converter.ts
│   ├── question-scorer.ts
│   ├── question-selector.ts
│   ├── README.md
│   ├── risk-message-generator.ts
│   ├── tag-confirmer.ts
│   ├── tag-estimate-connector.ts
│   ├── tag-process-mapper.ts
│   ├── tag-rules.ts
│   ├── types.ts
│   └── validator.ts
├── v5-ultimate/            # V5 Ultimate 분석
│   ├── dna-types.ts
│   ├── prompts.ts
│   ├── question-engine.ts
│   ├── types.ts
│   └── valueCalculator.ts
├── engine-v3/              # V3 엔진
├── engine-v3.1-core/       # V3.1 코어 엔진
├── decision-impact/        # 결정 영향 엔진
├── questions/              # 질문 정의
├── report/                 # 리포트 계산
└── ...
```

### 📁 app/api/v5/ 폴더 전체
```
app/api/v5/
├── analyze/
│   ├── chat/
│   │   └── route.ts        # 채팅 분석 API
│   ├── fusion/
│   │   └── route.ts        # 통합 분석 API
│   ├── grade/
│   │   └── route.ts        # 등급 분석 API
│   └── photo/
│       └── route.ts        # 사진 분석 API
└── generate-question/
    └── route.ts            # 질문 생성 API
```

---

## 3. 공정/견적/선택 관련 파일

### 🔍 "process" 또는 "공정" 포함 파일 (주요)

#### 페이지 파일
- `app/v5/process-select/page.tsx` - V5 공정 선택 페이지
- `app/v5/process-detail/page.tsx` - V5 세부 공정 선택 페이지
- `app/onboarding/process/page.tsx` - 온보딩 공정 선택 (구버전)
- `app/onboarding/process-new/page.tsx` - 온보딩 공정 선택 (신버전)
- `app/process-options/page.tsx` - 공정 옵션 페이지
- `app/process-select-pre/page.tsx` - 공정 선택 사전 페이지

#### 컴포넌트 파일
- `components/onboarding/process/SpaceAccordion.tsx` - 공간별 공정 아코디언
- `components/onboarding/process/TierOptionSelector.tsx` - 등급 옵션 선택기
- `components/onboarding/process/ConfirmModal.tsx` - 확인 모달

#### 스토어 파일
- `lib/store/processStore.ts` - 공정 선택 상태 관리

#### 유틸리티 파일
- `lib/utils/processMapper.ts` - 공정 매핑
- `lib/utils/processRecommender.ts` - 공정 추천

#### 설정 파일
- `lib/analysis/config/process-classification.ts` - 공정 분류 설정
- `lib/estimate/config/process-config.ts` - 공정 설정
- `constants/processes.ts` - 공정 상수
- `constants/process-definitions.ts` - 공정 정의

#### 타입 파일
- `types/process-options.ts` - 공정 옵션 타입
- `types/spaceProcess.ts` - 공간 공정 타입

### 🔍 "estimate" 또는 "견적" 포함 파일 (주요)

#### 페이지 파일
- `app/onboarding/estimate/page.tsx` - 견적 결과 페이지
- `app/estimate/page.tsx` - 견적 페이지

#### API 파일
- `app/api/estimate/v4/route.ts` - V4 견적 API
- `app/api/estimate/v3/route.ts` - V3 견적 API
- `app/api/estimate/constitution/route.ts` - 헌법 견적 API
- `app/api/estimate/calculate/route.ts` - 견적 계산 API
- `app/api/estimate/materials/route.ts` - 자재 견적 API
- `app/api/estimate/labor/route.ts` - 노무비 견적 API

#### 컴포넌트 파일
- `components/estimate/EstimateTable.tsx` - 견적 테이블
- `components/estimate/EstimateDetailTab.tsx` - 견적 상세 탭
- `components/estimate/EstimateSummaryCard.tsx` - 견적 요약 카드
- `components/estimate/EstimateAdvice.tsx` - 견적 조언

#### 라이브러리 파일
- `lib/estimate/calculator-v3.ts` - V3 견적 계산기
- `lib/estimate/constitution-estimate-engine.ts` - 헌법 견적 엔진
- `lib/estimate/rule-engine-v0.ts` - 룰 엔진 V0
- `lib/estimate-v4/` - V4 견적 시스템 전체
- `lib/utils/estimateCalculator.ts` - 견적 계산 유틸리티
- `lib/utils/estimate.ts` - 견적 유틸리티

#### 타입 파일
- `types/estimate.ts` - 견적 타입
- `lib/types/헌법_견적_타입.ts` - 헌법 견적 타입

### 🔍 "selection" 또는 "선택" 포함 파일 (주요)

#### 페이지 파일
- `app/v5/process-select/page.tsx` - 공정 선택
- `app/process-select-pre/page.tsx` - 공정 선택 사전

#### 컴포넌트 파일
- `components/v5-ultimate/GradeSelector.tsx` - 등급 선택기
- `components/v5-ultimate/MoodSelector.tsx` - 무드 선택기
- `components/onboarding/BudgetSelector.tsx` - 예산 선택기
- `components/ModeSelection.tsx` - 모드 선택

### 📁 app/onboarding/ 폴더 내용
```
app/onboarding/
├── ai-recommendation/
│   └── page.tsx            # AI 추천 페이지
├── ai-scope-recommendation/
│   └── page.tsx            # AI 범위 추천 페이지
├── detail-options/
│   └── page.tsx            # 세부 옵션 페이지
├── estimate/
│   └── page.tsx            # 견적 결과 페이지
├── personality/
│   └── page.tsx            # 성향 분석 페이지
├── process/
│   └── page.tsx            # 공정 선택 페이지 (구버전)
├── process-new/
│   └── page.tsx            # 공정 선택 페이지 (신버전)
└── scope/
    └── page.tsx            # 범위 선택 페이지
```

---

## 4. Git에서 최근 삭제된 파일 확인

### 검색 결과
```powershell
git log --diff-filter=D --summary --since="2024-12-01" | Select-String "delete mode"
```

**결과:** 2024-12-01 이후 삭제된 파일 없음

---

## 5. DB/타입 관련 파일

### 📁 Prisma 스키마
```
prisma/
├── schema.prisma           # Prisma 스키마 파일
└── dev.db                 # SQLite 데이터베이스
```

**주요 모델:**
- `User` - 사용자
- `Estimate` - 견적서
- `ItemMaster` - 품목 마스터 (200개 품목)
- `EstimateItem` - 견적 품목 상세
- `TraitWeight` - 성향 가중치 규칙

### 📁 lib/types/ 폴더
```
lib/types/
└── 헌법_견적_타입.ts      # 헌법 견적 타입 정의
```

### 📁 types/ 폴더 (루트)
```
types/
├── estimate.ts            # 견적 타입
├── image-generation.ts    # 이미지 생성 타입
├── process-options.ts     # 공정 옵션 타입
└── spaceProcess.ts        # 공간 공정 타입
```

### 📁 lib/estimate-v4/types/ 폴더
```
lib/estimate-v4/types/
├── estimate.types.ts      # 견적 타입
├── input.types.ts         # 입력 타입
├── personality.types.ts # 성향 타입
├── strategy.types.ts      # 전략 타입
├── v4-estimate-types.ts   # V4 견적 타입
└── index.ts              # 타입 인덱스
```

### 📁 lib/analysis/engine-v3.1-core/types/ 폴더
```
lib/analysis/engine-v3.1-core/types/
├── action.ts              # 액션 타입
├── input.ts               # 입력 타입
├── needs.ts               # 니즈 타입
└── resolution.ts          # 해결책 타입
```

### 📁 lib/analysis/v5-ultimate/ 폴더
```
lib/analysis/v5-ultimate/
├── types.ts               # V5 Ultimate 타입
└── dna-types.ts          # DNA 타입
```

---

## 6. 주요 파일 경로 요약

### V5 메인 플로우
1. **시작**: `app/v5/page.tsx`
2. **공정 선택**: `app/v5/process-select/page.tsx`
3. **세부 옵션**: `app/v5/process-detail/page.tsx`
4. **등급 선택**: `app/v5/options/page.tsx`
5. **견적 결과**: `app/onboarding/estimate/page.tsx`

### V5 API 엔드포인트
- 통합 분석: `app/api/v5/analyze/fusion/route.ts`
- 채팅 분석: `app/api/v5/analyze/chat/route.ts`
- 사진 분석: `app/api/v5/analyze/photo/route.ts`
- 등급 분석: `app/api/v5/analyze/grade/route.ts`
- 질문 생성: `app/api/v5/generate-question/route.ts`

### V5 스토어
- 통합 스토어: `lib/store/v5UltimateStore.ts`
- 공간 정보: `lib/store/spaceInfoStore.ts`
- 공정 선택: `lib/store/processStore.ts`

### V5 분석 엔진
- V5 Ultimate: `lib/analysis/v5-ultimate/`
- V5 코어: `lib/analysis/v5/`

---

## 7. 참고 사항

### 공정 선택 관련
- **V5 신버전**: `app/v5/process-select/` 사용
- **온보딩 구버전**: `app/onboarding/process/` 또는 `app/onboarding/process-new/` 사용
- 공정 옵션은 `app/v5/process-detail/`에서 세부 설정

### 견적 시스템
- **V4 시스템**: `lib/estimate-v4/` (최신)
- **V3 시스템**: `lib/estimate/calculator-v3.ts` (구버전)
- **헌법 시스템**: `lib/estimate/constitution-estimate-engine.ts`

### 타입 정의
- 대부분의 타입은 각 모듈 내부 `types.ts` 파일에 정의
- 공통 타입은 `types/` 폴더에 정의
- Prisma 모델은 `prisma/schema.prisma`에 정의

---

**마지막 업데이트**: 2024-12-XX




