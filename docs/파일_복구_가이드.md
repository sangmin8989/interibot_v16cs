# constitution-v1-engine.ts 파일 복구 가이드

**문제**: `lib/estimate/constitution-v1-engine.ts` 파일이 손상되었습니다.

**현재 상태**: 파일 크기 12바이트, 내용 "-NoNewline"만 존재

---

## 복구 방법

### 방법 1: IDE의 Undo 기능 사용 (권장)

1. Cursor IDE에서 `lib/estimate/constitution-v1-engine.ts` 파일 열기
2. `Ctrl + Z` (또는 `Cmd + Z`)를 여러 번 눌러서 이전 상태로 되돌리기
3. 파일이 복구될 때까지 Undo 실행

### 방법 2: 로컬 히스토리 사용

1. Cursor IDE에서 파일 우클릭
2. "Local History" 또는 "Timeline" 메뉴 선택
3. 이전 버전 선택하여 복구

### 방법 3: 다른 소스에서 복구

파일이 git에 추적되지 않았으므로, 다음 방법을 시도:

1. **백업 파일 확인**: `lib/estimate/constitution-v1-engine.ts.bak` 파일이 있는지 확인
2. **다른 브랜치 확인**: 다른 git 브랜치에 파일이 있는지 확인
3. **협업자 확인**: 다른 개발자가 파일을 가지고 있는지 확인

---

## 임시 해결책

파일이 복구되지 않는 경우, 다음 작업이 필요합니다:

1. **헌법 v1.1 전용 서비스는 정상 작동**: `material-service-strict.ts`, `labor-service-strict.ts`는 정상
2. **API 라우트는 정상**: `app/api/estimate/constitution/route.ts`는 정상
3. **파일 재작성 필요**: `constitution-v1-engine.ts` 파일을 다시 작성해야 함

---

## 참고사항

- 파일 손상 원인: PowerShell 명령어 실행 중 오류
- 영향 범위: `constitution-v1-engine.ts` 파일만 손상
- 다른 파일: 정상 작동 중

---

**즉시 조치**: Cursor IDE에서 `Ctrl + Z`를 눌러 파일을 복구하세요.




