# 🔧 인테리봇 업그레이드 가이드

이 문서는 인테리봇에 **새로운 공정이나 옵션을 추가**하는 방법을 설명합니다.

---

## 📁 파일 구조

```
lib/estimate/
├── config/
│   └── process-config.ts    ← ✅ 이 파일만 수정하세요!
├── unified-calculator-v2.ts  ← 계산 로직 (자동 반영)
└── types.ts                  ← 타입 정의 (참고용)

lib/data/
└── estimate-master-v2.ts     ← 가격 데이터 (필요시 수정)
```

---

## 🚀 1. 새 옵션 추가하기 (가장 쉬움)

### 예시: 주방에 "정수기 위치" 옵션 추가

**파일:** `lib/estimate/config/process-config.ts`

```typescript
주방: {
  options: {
    // ... 기존 옵션들 ...
    
    // ✅ 여기에 새 옵션 추가!
    정수기위치: {
      type: 'select',
      label: '정수기 위치',
      values: ['싱크대 밑', '벽면', '별도공간'] as const,
      default: '싱크대 밑'
    }
  }
}
```

### 옵션 타입 종류

| 타입 | 설명 | 예시 |
|------|------|------|
| `boolean` | 예/아니오 | `{ type: 'boolean', label: '식기세척기', default: false }` |
| `select` | 하나 선택 | `{ type: 'select', values: ['A', 'B', 'C'] as const, default: 'A' }` |
| `multiselect` | 여러 개 선택 | `{ type: 'multiselect', values: ['A', 'B', 'C'] as const, default: ['A'] }` |

### 조건부 옵션 (다른 옵션에 따라 표시)

```typescript
욕조타입: {
  type: 'select',
  values: ['일반', '반신욕', '자쿠지'] as const,
  default: '일반',
  dependsOn: { 욕조: true }  // ← 욕조가 true일 때만 표시
}
```

---

## 🏗️ 2. 새 공정 추가하기

### 예시: "스마트홈" 공정 추가

**파일:** `lib/estimate/config/process-config.ts`

```typescript
export const PROCESS_CONFIG = {
  // ... 기존 공정들 ...

  // ✅ 새 공정 추가!
  스마트홈: {
    code: '1100',           // 고유 코드 (다른 공정과 중복 안 됨)
    name: '스마트홈',
    description: '스마트홈 시스템 설치',
    spaces: ['common'],     // 적용 공간 (common = 전체)
    
    options: {
      시스템타입: {
        type: 'select',
        label: '시스템 타입',
        values: ['기본', '프리미엄', '풀옵션'] as const,
        default: '기본'
      },
      음성비서: { type: 'boolean', label: '음성비서', default: false },
      조명제어: { type: 'boolean', label: '조명 제어', default: true },
      온도제어: { type: 'boolean', label: '온도 제어', default: true },
      보안시스템: { type: 'boolean', label: '보안 시스템', default: false }
    }
  }
}
```

---

## 💰 3. 가격 데이터 추가하기

새 공정/옵션을 추가했으면 가격 데이터도 추가해야 합니다.

**파일:** `lib/data/estimate-master-v2.ts`

```typescript
스마트홈: {
  항목: [
    {
      항목명: '스마트홈 기본 패키지',
      규격: '조명+온도 제어',
      단위: '식',
      수량계산: { 기준: '고정', 계수: 1 },
      재료비: { basic: 500000, standard: 800000, argen: 1200000, premium: 2000000 },
      노무비: { basic: 200000, standard: 300000, argen: 400000, premium: 600000 },
      브랜드: { basic: '국산', standard: '삼성', argen: 'LG', premium: '구글/애플' },
      spaces: ['common']
    },
    // 조건부 항목
    {
      항목명: '음성비서 설치',
      단위: 'EA',
      수량계산: { 기준: '고정', 계수: 1 },
      재료비: { basic: 100000, standard: 200000, argen: 350000, premium: 500000 },
      노무비: { basic: 50000, standard: 50000, argen: 50000, premium: 50000 },
      브랜드: { basic: '국산', standard: '구글홈', argen: '애플홈팟', premium: '보스' },
      조건: { 스마트홈옵션: { 음성비서: true } },  // ← 조건 추가
      spaces: ['common']
    }
  ]
}
```

---

## 📝 4. 가격 배율 추가하기

특정 옵션이 가격에 배율을 적용해야 하는 경우:

**파일:** `lib/estimate/config/process-config.ts`

```typescript
형태: {
  type: 'select',
  label: '주방 형태',
  values: ['일자', 'ㄱ자', 'ㄷ자', '아일랜드'] as const,
  default: '일자',
  priceMultiplier: {      // ← 배율 추가
    '일자': 1.0,
    'ㄱ자': 1.15,
    'ㄷ자': 1.35,
    '아일랜드': 1.5
  }
}
```

---

## ✅ 체크리스트

새 공정/옵션 추가 시 확인사항:

- [ ] `process-config.ts`에 옵션 추가
- [ ] `estimate-master-v2.ts`에 가격 데이터 추가
- [ ] 배율이 필요하면 `priceMultiplier` 추가
- [ ] 조건부 항목이면 `조건` 또는 `dependsOn` 추가
- [ ] `npm run build` 실행하여 에러 없는지 확인

---

## 🔗 연관 파일 (참고용)

| 파일 | 설명 |
|------|------|
| `lib/estimate/types.ts` | 타입 정의 (타입 추가 시 여기도 수정) |
| `lib/estimate/unified-calculator-v2.ts` | 계산 로직 (특수 계산 로직 추가 시) |
| `lib/analysis/ai-analyzer.ts` | AI 분석 (성향 → 옵션 연동 시) |

---

## ❓ 도움이 필요하면

Cursor AI에게 물어보세요:
- "주방에 새 옵션 추가해줘"
- "스마트홈 공정 추가하고 싶어"
- "옵션 X가 선택되면 가격 20% 올려줘"





























